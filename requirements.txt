# Core runtime for Section-2 classical campaign + VQT (Leonardo CPU path).
# Install on login node:
#   bash hpc_setup_env.sh
# or:
#   /leonardo_work/IscrC_QuSALa/venv_py311/bin/python -m pip install -r requirements.txt

pip>=24.0
setuptools>=68.0
wheel>=0.43

numpy>=1.26,<3
scipy>=1.11
# CPU JAX (training / baselines use JAX_PLATFORMS=cpu)
jax>=0.4.30
jaxlib>=0.4.30
pennylane>=0.38
pennylane-lightning>=0.38
mpi4py>=3.1
matplotlib>=3.8
psutil>=5.9
